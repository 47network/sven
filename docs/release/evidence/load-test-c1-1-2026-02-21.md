# Load Test Evidence: C1.1 Baseline (2026-02-21)

## Scope

- Checklist section: `C1.1 Load Testing`
- Script: `tests/load/gateway-load-test.js`
- Output artifact: `docs/performance/load-test-results.json`

## Command

```powershell
docker run --rm -v "${PWD}:/work" -w /work grafana/k6 run `
  --env BASE_URL=http://host.docker.internal:3000 `
  --env COOKIE="sven_session=<admin-session>" `
  --env ADAPTER_TOKEN="sven-local-dev-adapter-token-change-me-in-production" `
  tests/load/gateway-load-test.js
```

## Result Summary

- Run status: **failed** (thresholds crossed)
- `http_req_duration.p(95)`: `8337.17ms` (target `<500ms`)
- `errors_5xx.count`: `7323` (target `0`)
- `http_req_failed.rate`: `74.16%` (target `<0.1%`)
- Max VUs reached: `100`
- Concurrent chat sessions scenario reached: `10`
- LLM first-token probe: **not run** (no OpenAI-compatible probe env vars set)

## Observed Failure Signal

`gateway-api` logs showed sustained Postgres timeout errors during/after load:

- `timeout exceeded when trying to connect`
- `Connection terminated due to connection timeout`

## Follow-up

Checklist C1.1 performance targets remain open pending:

- DB/pool tuning and/or query-path optimization under 100 VU
- re-run with same profile until p95 + 5xx targets pass
- separate 24h RSS stability run
- optional LLM first-token run with `OPENAI_API_KEY` + `OPENAI_MODEL`

