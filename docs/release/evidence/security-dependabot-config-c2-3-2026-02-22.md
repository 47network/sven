# C2.3 Dependency Update Automation (Dependabot) (2026-02-22)

Date: 2026-02-22  
Owner: Codex session

## Change

Added Dependabot configuration:

- `.github/dependabot.yml`

Coverage configured:

- npm (`/`) weekly
- GitHub Actions (`/`) weekly
- Docker (`/` + service directories with Dockerfiles) weekly

## Outcome

Repository now has automated dependency update automation in place (Dependabot).
