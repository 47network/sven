{
  "name": "@sven/sven-eidolon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sven Eidolon Service — read-only aggregation over agent-runtime / treasury / marketplace / infra state. Feeds the eidolon.sven.systems 3D city via snapshot + SSE event stream.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --noCheck",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@sven/shared": "workspace:*",
    "fastify": "^5.8.5",
    "nats": "^2.29.3",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "tsx": "^4.21.0",
    "typescript": "catalog:"
  }
}
