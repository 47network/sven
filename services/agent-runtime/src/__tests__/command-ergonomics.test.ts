import { describe, expect, it } from '@jest/globals';

describe('command ergonomics', () => {
  it('supports directive form (sven: version)', () => {
    expect(true).toBe(true);
  });

  it('supports configurable command prefix', () => {
    expect(true).toBe(true);
  });

  it('keeps /skill and /skills aliases equivalent', () => {
    expect(true).toBe(true);
  });
});
