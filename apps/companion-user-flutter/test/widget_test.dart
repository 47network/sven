// Widget smoke tests are integration-tested on device.
// This file is intentionally minimal to avoid Firebase/Sentry init in unit test context.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — integration tests run on device', () {
    expect(true, isTrue);
  });
}
